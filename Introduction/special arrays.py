import numpy as np

A = np.zeros((2, 3))
# 2 x 3 matrix where every elements are 0
print(A)

B = np.ones((2, 2))
# 2 x 2 matrix where every elements are 1
print(B)

C = np.full((3, 2), 3)
# 3 x 2 matrix where every elements are 3
print(C)

D = np.eye(2)
# 2 x 2 identity matrix
print(D)