import numpy as np

u = np.array([-3, 1, 7, -5])
v = np.array([9, 2, -4 , 1])

print(np.dot(u, v))

u = np.array([-1, 2, 5, -3])
v = np.array([2, -3, -1 , 5])

print(np.dot(u, v))

u = np.array([2, 3, -1])
v = np.array([5, 1, -2])

print(np.dot(u, v))
